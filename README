PSU Search 7.20-dev

The project has also been renamed to psu_search .

This is a forked version of the original implimentation which used a template
file to render the form and javascript to control the functionality.

This version utilizes the Form API to display render the form and control
the functionality.

ToDo:
- Responsive CSS à la the http://www.psu.edu search block.
- Properly define the global variables in the module.
- Check for accessiblity.
- Add CSS/JS toggle in block configuration.